services:
  - type: web
    name: cbc-api
    env: python
    plan: free
    buildCommand: |
      apt-get update && apt-get install -y openscad
      openscad --version
      pip install -r requirements.txt
    startCommand: "gunicorn app:app"
    envVars:
      - key: FLASK_ENV
        value: production